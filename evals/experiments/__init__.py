"""Experiments on the ten audited questions.

Each module is one intervention, each has a matching .md reporting what it did
and what happened. `_bench.py` is the shared harness so every experiment scores
the same ten questions the same way.
"""
