# CPU-only torch: the NLI fine-tune runs on CPU by design (see main.py), and the
# CUDA wheels would add several GB to the image for nothing.
--extra-index-url https://download.pytorch.org/whl/cpu

# Pinned to the project venv's versions so serving reproduces the evaluated
# numbers (transformers version changes predictions).
torch==2.9.1
transformers==5.9.0
fastapi==0.121.2
uvicorn[standard]==0.38.0
pydantic==2.12.3
sentencepiece==0.2.1
protobuf==6.33.1
